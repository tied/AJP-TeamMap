		
       ##foreach($issue in $issues)
       		##$issue.getId()<br>
       		##set($issueChanges=$changeHistory.get($issue))
       		##foreach($issueC in $issueChanges)
       			##$issueC<br>
       		##end
       		##<br>
       ##end
       
       #foreach($issue in $issues)
       		#$issue.getKey()<br>
       		$changeHistory.get($issue)
       		
       		<br>
       #end